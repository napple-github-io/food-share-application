// const User = require('../models/Listing');
// const mongoose = require('mongoose');

// function powerCheck() {
//   return User
//     .getPowerUsers()
//     .then(list => {
//       list.map(listing => {
//         return User
//           .findByIdAndUpdate(listing._id, { powerUser: true }, { new: true });
//       });
//     });
// }

// module.exports = powerCheck;
